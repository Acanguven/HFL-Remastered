'use strict';
/**
 * @ngdoc function
 * @name sbAdminApp.controller:account
 * @description
 * # MainCtrl
 * Controller of the sbAdminApp
 */
angular.module('sbAdminApp').controller('account', function($rootScope,$scope) {
    if(!$rootScope){
    	return false;
    }
    
});