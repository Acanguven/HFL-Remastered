'use strict';
/**
 * @ngdoc function
 * @name sbAdminApp.controller:banners
 * @description
 * # MainCtrl
 * Controller of the sbAdminApp
 */
angular.module('sbAdminApp').controller('banners', function($rootScope,$scope) {
    if(!$rootScope){
    	return false;
    }
    
});