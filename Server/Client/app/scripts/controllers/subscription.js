'use strict';
/**
 * @ngdoc function
 * @name sbAdminApp.controller:subscription
 * @description
 * # MainCtrl
 * Controller of the sbAdminApp
 */
angular.module('sbAdminApp').controller('subscription', function($rootScope,$scope) {
    if(!$rootScope){
    	return false;
    }
    
});